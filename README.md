# context-type-system
A pure-Python runtime layer for typed AI agent context — instructions, evidence, memory, and tool output stay explicitly typed and provenance-tracked until the moment they're serialized into a prompt.
